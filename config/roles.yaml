roles:
  llm.preprocess:
    type: llm

  llm.mem0:
    type: llm

  llm.openmemory:
    type: llm

  llm.embedding.default:
    type: llm

  store.vector.facts:
    type: vector

  store.vector.cognitive:
    type: vector
